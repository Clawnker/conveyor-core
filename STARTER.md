# Starter implementation checklist

1. Add `/intake/capture` endpoint for external channel/message capture.
2. Add `/drills/canary` and `/drills/report` endpoints.
3. Implement HMAC actor identity enforcement for write routes.
4. Add repo allowlist env support.
5. Add UI controls for:
   - Run canary
   - View report
   - Capture intake from message link
   - Create no-repo template card
